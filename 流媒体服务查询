// 示例测试用例
describe('Netflix Detector', () => {
  test('should detect US region', async () => {
    const mockResponse = {
      statusCode: 200,
      headers: { 'X-Originating-URL': 'https://www.netflix.com/title/81280792' }
    };
    const result = StreamTest._parseNetflix(mockResponse);
    expect(result.region).toBe('US');
  });
});